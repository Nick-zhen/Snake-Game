#include "Game.hpp"

int main()
{
    // implement in stack
    Game game;
    game.run();
    // implement in heap;
    // keyword new will return a pointer
    // Game* game = new Game();
    // game->Run();
    // delete game;

    return 0;
}